# Database (SQLite — relative to project root)
DATABASE_URL="file:./data/productivity-audit.db"

# API Protection (generate with: openssl rand -hex 32)
API_SECRET=

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
